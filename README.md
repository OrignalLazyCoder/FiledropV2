# FiledropV2

Requirements to run this:
1. Ganache by truffle running on port 7545
2. truffle installed using npm globally
3. Solidity compiler installed
4. nodeJS 10.x.x +
5. npm
6. VsCode


Use `npm install` in root directory before using `npm start` command
